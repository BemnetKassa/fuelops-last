import type { NextConfig } from "next";

const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ["http://10.144.56.37:3000"],
} as NextConfig;

export default nextConfig;
